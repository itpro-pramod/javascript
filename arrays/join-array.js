const numbers = [1,2,3,4];

const join = numbers.join(',');

console.log(join);

const message = "this is my first program";
const part = message.split(' ');
console.log('part :>> ', part);
const partjoins = part.join('-');
console.log(partjoins);